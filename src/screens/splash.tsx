import {
   Image, 
   StyleSheet
} from 'react-native'

import Screen from '@components/shared/screen' 

export default (): JSX.Element => {
   return (
      <Screen full>
         
      </Screen>
   )
}

const styles = StyleSheet.create({

})
