import {
   View, 
   Text, 
   TextInput,
   StyleSheet
} from 'react-native'

export default (): JSX.Element => {
   return (
      <View style={styles.container}>

      </View>
   )
}

const styles = StyleSheet.create({
   container: {
      flex: 1,
      alignItems: 'center'
   }
})